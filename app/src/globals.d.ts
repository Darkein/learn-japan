// Constantes injectées à la compilation par Vite (`define` dans vite.config.ts).

/** Identifiant du build (SHA git court + date UTC), affiché en bas des réglages. */
declare const __BUILD_ID__: string;

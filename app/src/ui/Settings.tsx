import { SettingsSections } from "./SettingsSections";

/** Page « Tous les paramètres » : toutes les sections, y compris la Révision. */
export function Settings() {
  return <SettingsSections />;
}
